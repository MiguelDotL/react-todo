var React = require('react'),

    Home = React.createClass({
      render: function() {
        return(
          <div>
            <h1 className="page-title">Home</h1>
          </div>
        );
      }
    });

module.exports = Home;
