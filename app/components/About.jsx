var React = require('react'),

    About = React.createClass({
      render: function() {
        return(
          <div>
            <h1 className="page-title">About</h1>
          </div>
        );
      }
    });

module.exports = About;
